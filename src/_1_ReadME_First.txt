Open up all the .js files in the Layout, Pages, and Users folder
Make sure the Post/Get/Put/Delete mapping are the correct URL corresponding to the backend Intellij method's URL
use terminal to run via 'npm start', will tell you which local host port (most likely default 3000)
check the controller file on Intellij to make sure the 'CrossOrigin(URL)' is annonated to the React localhost port 
test
